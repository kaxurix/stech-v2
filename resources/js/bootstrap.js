/**
 * resources/js/bootstrap.js
 *
 * Standard Laravel JavaScript bootstrap file.
 * Axios is not required for this Inertia-only prototype,
 * but the file must exist so the import resolves.
 */

// No-op — Inertia handles all XHR internally via fetch.
// Add axios / pusher / echo config here if needed later.
